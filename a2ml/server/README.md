# Development

## Deps

Install these packages:
```
fastapi
uvicorn
redis
asyncio
asyncio-redis
```

## Test

Run dev server:
```
cd a2ml/server
uvicorn main:app --reload
```

Go to http://localhost:8000/ and click `Start transaction`
