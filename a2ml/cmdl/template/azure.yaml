---
# experiment settings unique to Azure, currently just metric used for scoring
experiment:
  metric: AUC_weighted
  run_id:

cluster:
  region: eastus2
  min_nodes: 0
  max_nodes: 2
  type: STANDARD_D2_V2
