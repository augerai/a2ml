{
    "version": "0.2.0",
    "configurations": [
        { "name": "Python: Terminal (integrated)", 
            "type": "python",
             "request": "launch", 
             "pythonPath": "${config:python.pythonPath}", 
             "program": "${file}", 
             "cwd": "~/a2ml/tests/test_google", 
             "console": "integratedTerminal", 
             "env": {}, 
             "args": ["import"], 
             "envFile": "~/Envs/a2ml", 
        }
    ]
}